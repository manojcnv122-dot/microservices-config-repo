management:
  endpoints:
    web:
      exposure:
        include: ["health", "info", "prometheus", "*"]
  endpoint:
    prometheus:
      enabled: true
  metrics:
    enable:
      all: true

management:
  tracing:
    sampling:
      probability: 1.0
